export default function Profiles() {
    return(
        <div>
         <h1>Profiles page</h1>
        </div>
    )
}

//if page exists on a same level of route then route file run
// we can handle route in seperate api folder