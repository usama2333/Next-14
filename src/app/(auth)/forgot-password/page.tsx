export default function ForgotPassword() {
    return <h1>Forgot Password</h1>
}

// by placing brackets outside (auth) it will group routes then we no need
// then we no need to write auth in the url