export default function InterceptedAbout() {
    return(
        <>
        <h1>(...) Intercepted about Page</h1>
        
        </>
    ) 
}

//(...) is used to go back to root directory to match route 
// like /about is in root directory so we use (...)